{{#each componentSetNodes}}
/* BEGIN_FILE: {{pascalcase this.name}}.tsx */
import { FC } from 'react';

interface Props {
{{#each (mapProperties this.variantGroupProperties)}}
  {{this.property}}:{{#if this.isBoolean}} boolean;{{/if}}{{#if this.isNumber}} number;{{/if}}{{#if this.isString}}{{#each this.values}} | '{{this}}'{{/each}};{{/if}}
{{/each}}
}

const {{pascalcase this.name}}: FC<Props> = (props: Props) => {
  return null;
};

export default {{pascalcase this.name}};
export { Props as {{pascalcase this.name}}Props };
{{/each}}
